# Decisions log

- [2026-07-05] Use existing `.venv` (Python 3.13) — satisfies the 3.11+ requirement; torch already installed, avoids a second heavy install. `uv`/`pnpm` unavailable → pip + npm per decision table fallback.
- [2026-07-05] Moved `data/` and `runs/` under `research/` — they are research artifacts; scripts use CWD-relative paths and run from `research/` unchanged. CSVs are gitignored (450 MB).
- [2026-07-05] Single root `pyproject.toml` governs engine+api (one venv, one tool config) instead of per-package projects — simplest monorepo layout that keeps `research/` untouched.
- [2026-07-05] Feature count F=14 fixed as: quote_imb, flow_imb, rel_spread, logret_1, logret_3, logret_6, logret_12, vwap_dev, vol_z, range_atr, tradecount_z, rv_pctile, tod_sin, tod_cos (spec said "~14").
- [2026-07-05] Approval-token enforcement: OrderManager.submit(approval: Approval); Approval only constructible via RiskManager._issue — structural §1.6 guarantee.
- [2026-07-05] LOW-tier daily rebalance approximated as rebalance_seconds=86400 within the entry window rather than a 15:45 ET scheduler — simplest correct behavior; revisit if LOW is used heavily.
- [2026-07-06] Web: browser gets the WS token from a cookie-gated same-origin /api/ws-token route (httpOnly cookie can't be read by JS; the backend WS requires a query token). Single-user tradeoff.
- [2026-07-06] Playwright smoke mocks the backend at the network layer (page.route) so it runs standalone; cookie is seeded explicitly because route.fulfill set-cookie doesn't populate the browser jar. Real auth path covered by API tests.
- [2026-07-06] lightweight-charts v5 API (addSeries(LineSeries, ...)) used instead of the v4 addLineSeries in older docs.
- [2026-07-06] Alpaca Basic plan caps ws subscriptions at 30/connection (hit in first live run: 20 symbols x trades+quotes = 40 -> 405 -> staleness kill, safety worked). Live path now subscribes the official 1-min BAR channel for all symbols and spends leftover budget on quotes then trades for leading symbols; local BarBuilder remains for tests/replay. Symbols without quote/trade subs carry zero spread/imbalance/flow features.\n- [2026-07-09] Root-caused "zero trades in 3 live days" (720 ensemble computes, max |score| 0.434 vs gates 0.52–0.60). Three compounding fixes: (1) training.py's directional_accuracy was recall-style — FLAT predictions on moving bars counted as failures — writing 0.3276 into lob_flow_metrics.json and zeroing lob_flow's confidence since export; redefined prediction-conditioned (matches evaluate_signal.simulate_trades), same model/CM scores 0.5392 → signal revived at full confidence. Old value kept as directional_recall_nonflat. (2) momentum & mean_reversion opposed sign ~44% of the time both were active and self-cancelled; their pooled tier weight is now reallocated per compute by Kaufman efficiency ratio over MR's 2h window (ensemble.trend_weight, ER 0.10→0.35 maps all-MR→all-momentum). (3) tier gates recalibrated to the achievable post-blending distribution: LOW 0.60→0.55, MED 0.55→0.45, HIGH 0.52→0.40.
- [2026-07-09] Execution split by Approval.urgent (issued by approve_exit only). Entries are maker-only: limit at mid∓spread/2, one re-peg to mid after 30s, then EXPIRE — never market (research/cost_aware_experiment.py: taker PnL negative on every stock/horizon even with perfect foresight; maker positive on all). Exits keep limit-toward-touch → market fallback: certainty of getting flat beats spread cost.
- [2026-07-09] Strategic pivot for the "beat SPY" goal: intraday 1-min ensemble on mega-caps is structurally the wrong arena for a retail account (most-arbed instruments, free delayed data, no real LOB depth live, tiny single-regime training set) — keep it as a research/paper track, but the primary alpha becomes DAILY cross-sectional momentum, built in `research/xsec/` (Alpaca SIP daily bars 2016→, adjustment=all so closes are total-return; current-constituent universe with survivorship bias controlled via EW-universe benchmark + liquidity-at-the-time eligibility filter). Walk-forward OOS 2020–2026.5, net of 10bps one-way: plain 12-1 momentum top-50 monthly CAGR 28.0% / Sharpe 1.03 / IR 0.77 vs SPY 15.8% / 0.83 (liquidity-filtered; unfiltered 33.2% is inclusion-bias-inflated). HistGBT ranker on 7 factors UNDERPERFORMS plain momentum OOS (21.6% net, 2.3x turnover) — 55k monthly samples can't support learned interactions; keep the ML as ranker-baseline, ship the simple factor. Expected live edge should be discounted to roughly +3–6%/yr over SPY (residual survivorship + momentum-crash risk, 28% vol). Next step (not yet built): port as a LOW-tier daily-cadence xsec_momentum signal with monthly top-N rebalance.
- [2026-07-09] Scaled tournament (research/scripts/run_xsec2.py): 2,908 liquid US stocks (screened from 12,719 Alpaca actives), 223,660 samples, 15 factors, five contenders (momentum / GBT / torch-MLP seed-ensemble / teacher-ensemble / distilled ridge), walk-forward OOS 2020–2026.5. VERDICT: (1) survivorship bias in a today's-liquidity-screened universe is ~21%/yr (EW 37.0% CAGR vs SPY 15.8%) — absolute returns there are meaningless; judge only vs same-bias EW. (2) Momentum adds just +2.8%/yr (IR 0.10) over EW in that universe; every ML ranker has IC ≈ 0 and subtracts value (GBT −16.5%/yr vs EW, IR −0.90). More breadth did NOT rescue ML with price/volume-only features at monthly horizon — the literature's ML edge needs fundamentals/alt-data, which we don't have free. (3) Deployable strategy remains large-cap S&P-universe 12-1 momentum top-50 monthly (28% net backtest, honest expectation +3–6%/yr over SPY). (4) LLM distillation shipped: Claude labeled 682 Benzinga headlines in-session (data/news_labels_teacher.csv), TF-IDF+logistic student = 74.3% held-out agreement, 5.1% sign-flip (models/news_sentiment.pkl, research/xsec/sentiment.py); it is a live-scoreable FEATURE CANDIDATE — no alpha claim until news backfill validates it in the walk-forward harness. Vol-managed overlay reduced Sharpe in-sample here; not adopted.
